import SwiftUI

struct ProfileView: View {
    var body: some View {
        VStack {
            Text("Welcome to AI Gear")
                .font(.title)
                .padding()

            // Add more UI components here
        }
    }
}
